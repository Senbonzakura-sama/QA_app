{% include 'default/_form.html.twig' with {
    page_title: 'title.question_edit'|trans({'%id%': question.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'question_index',
} %}